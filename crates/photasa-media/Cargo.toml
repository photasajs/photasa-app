[package]
name = "photasa-media"
version = "3.0.0"
edition = "2021"
description = "Unified image/video extension checking logic (Photasa)"
license = "MIT"

[dependencies]
photasa-types = { workspace = true }
